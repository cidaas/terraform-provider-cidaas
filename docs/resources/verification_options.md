---
page_title: "cidaas_verification_options Resource - cidaas"
subcategory: "Security"
description: |-
  Manages verification options via verification-actions-srv/verification-options. Apps reference this via authentication_setup.verification_options_id. Requires scopes cidaas:verification_read, cidaas:verification_write, cidaas:verification_delete.
---

# cidaas_verification_options (Resource)

Manages verification options via `verification-actions-srv/verification-options`. Apps reference this via `authentication_setup.verification_options_id`. Requires scopes `cidaas:verification_read`, `cidaas:verification_write`, `cidaas:verification_delete`.

## Example Usage

```terraform
# Example: cidaas_verification_options Resource (v4 Trustdesk)
#
# Configures MFA verification options and links to suggest_verification_method configurations.

resource "cidaas_suggest_verification_method" "web" {
  name        = "web-suggest-methods"
  description = "Standard suggest flow for interactive web apps"

  suggest_verification_method = {
    mandatory_config = {
      methods = ["EMAIL"]
      range   = "ONEOF"
    }
    optional_config = {
      methods = ["TOTP"]
    }
    skip_duration_in_days = 7
  }
}

resource "cidaas_verification_options" "web" {
  name        = "web-verification-options"
  description = "Verification options for web apps"

  verification_options = {
    setting                        = "ALWAYS"
    allowed_methods                = ["EMAIL", "SMS"]
    use_default_password_policy    = true
    suggest_verification_method_id = cidaas_suggest_verification_method.web.id
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `description` (String)
- `name` (String)
- `verification_options` (Attributes) (see [below for nested schema](#nestedatt--verification_options))

### Read-Only

- `id` (String) The ID of this resource.
- `owner` (String)

<a id="nestedatt--verification_options"></a>
### Nested Schema for `verification_options`

Optional:

- `allowed_methods` (List of String)
- `app_attest` (Attributes) (see [below for nested schema](#nestedatt--verification_options--app_attest))
- `password_policy_ref` (String)
- `setting` (String)
- `suggest_verification_method_id` (String) ID of a `cidaas_suggest_verification_method` resource.
- `time_interval_in_seconds` (Number)
- `use_default_password_policy` (Boolean)

<a id="nestedatt--verification_options--app_attest"></a>
### Nested Schema for `verification_options.app_attest`

Optional:

- `android` (Attributes) (see [below for nested schema](#nestedatt--verification_options--app_attest--android))
- `ios` (Attributes) (see [below for nested schema](#nestedatt--verification_options--app_attest--ios))

<a id="nestedatt--verification_options--app_attest--android"></a>
### Nested Schema for `verification_options.app_attest.android`

Required:

- `provider` (String)

Optional:

- `android_app_id` (String)
- `cert_digests` (List of String)
- `gcp_service_account` (String, Sensitive)
- `ios_app_id` (String)
- `project_id` (String)
- `relax_app_recognition` (Boolean)


<a id="nestedatt--verification_options--app_attest--ios"></a>
### Nested Schema for `verification_options.app_attest.ios`

Required:

- `provider` (String)

Optional:

- `apple_root_cert` (String, Sensitive)
- `gcp_service_account` (String, Sensitive)
- `ios_app_id` (String)
- `project_id` (String)
